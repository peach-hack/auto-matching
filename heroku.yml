setup:
  addons:
    - plan: heroku-postgresql
    - plan: radistogo
build:
  docker:
    web: auto-matching-api-rails/Dockerfile
    worker: auto-matching-api-rails/Dockerfile
run:
  web: bin/boot_rails
  worker: bin/boot_sidekiq

