development:
  adapter: redis

test:
  adapter: redis

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_PROVIDER") { "redis://localhost:6379/1" } %>
  channel_prefix: auto-matching-api-rail_production
